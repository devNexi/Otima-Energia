1. Oscar's view — redesign to match the quality of the rest of the prototype
Keep all the data and functionality exactly as it is. Change only the visual design and layout.
Oscar's Meus Alvos page needs to match the same card quality as the girls' queue. Each lead card should:
Use the same rounded-xl white card with shadow-sm and hover shadow-md as the rest of the prototype. Left border 4px in status colour — purple for Reunião Marcada, blue for Proposta Enviada, amber for Em contato, green for Agente Ativo, grey for Não contatado.
Inside the card: top row = company name bold + score badge right (same style as priority score). Second row = business type chip + city/state + owner name with person icon. Third row = portfolio estimate shown as a highlighted stat — 'Portfólio estimado: ~300 clientes PJ' in a small pill. Fourth row = status chip (coloured) + last contact date. Fifth row if notes exist: notes preview in muted italic. Bottom row = action buttons matching the girls' style: 'Enriquecer' (purple, with sparkle icon) / 'Abrir' / 'WA' / 'Email'.
The filter chips at the top (Reunião Marcada 2 / Proposta Enviada 1 etc) should use the same soft fill badge style as the rest of the prototype — not outlined.
The search bar should match the girls' queue search bar exactly.
The pipeline view (Pipeline de Agentes) should be a proper kanban board — same visual language as the Board view on the girls' queue. Five columns: Prospecção / Em Conversa / Reunião / Proposta Ótima Agente / Agente Ativo. Each column has a header with count badge. Cards inside are compact versions of the lead cards.
Oscar's lead card when opened should use exactly the same two-column layout as the girls' lead card — left column for company info, right area for tabs. The left column design should be identical. The tabs (Inteligência do Negócio / Enriquecimento DM / Mensagens / Histórico) should use the same tab switcher design. The Enriquecimento DM tab should use the identical DMEnrichmentPanel component.